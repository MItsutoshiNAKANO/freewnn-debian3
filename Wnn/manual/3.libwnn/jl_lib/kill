


JL_KILL( )          UNIX Programmer's Manual           JL_KILL( )



名称

     jl_kill()           文節を削除する。

形式

     #include "jllib.h"

     int
     jl_kill(buf, bun_no, bun_no2)
     struct wnn_buf *buf;          /* バッファ */
     int bun_no;                   /* 文節番号1 */
     int bun_no2;                  /* 文節番号2 */

機能

     文節番号 bun_no から文節番号 bun_no2 の前までの文節を削除す
     る。

     文節番号は、 0 から始まる。文節の最後を指定する時は、
     bun_no2 に -1 を指定する。

リターンバリュー

     文節数を返す。




Printed 1/22/92         20 September 1991                       1































































































