


JS_CLOSE( )         UNIX Programmer's Manual          JS_CLOSE( )



名称

     js_close()           日本語マルチクライアントサーバの使用を終了する。

形式

     #include "jslib.h"

     int
     js_close(server_id)
     WNN_JSERVER_ID *server_id;    /* サーバID */

機能

     サーバ ID server の使用を終了し、サーバとの接続を切る。再度
     日本語マルチクライアントサーバを使用するためには、 js_open
     を行う。登録可能形式辞書、及び、頻度情報のセーブは、行わない。

リターンバリュー

     正常終了時には 0 を返す。
     異常終了時には -1 を返す。




Printed 1/22/92         20 September 1991                       1


































































































