


WDDEL( )            UNIX Programmer's Manual             WDDEL( )



名称

     wddel               単語削除/コメント/品詞セットをバッチ的に行なう。

形式

     /usr/local/bin/Wnn4/wdreg [-D ホスト名] [-n 環境名] [-d 辞書番号]
             [-H|C|E] [-L] [ファイル名] < テキスト

機能

     指定された jserver の環境の辞書から、 text の中の番号の単語
     をバッチ的に削除する。 text は、各行の先頭に、削除したい単語
     の serial no が書かれているファイルであること。

     以下のオプションによって、動作を指定する。

     -D ホスト名
             サーバのホスト名を指定する。指定されない場合、環境変
             数 JSERVER が参照される。これもない場合は、
             UNIX_domain が使われる。

     -n 環境名
             環境名を指定する。デフォルトは "wddel" である。

     -d 辞書番号
             辞書のシリアル番号を指定する。ファイル名と、このオプ
             ションは同時には指定できない。ファイル名かこのオプシ
             ョン、どちらかを指定する。

     -C      コメントをセットする。

     -H      品詞をセットする。

     -E      単語を削除する。 ( デフォルト )

     -L      クライアント側の辞書が参照される。



Printed 1/28/92          28 January 1992                        1




















































































