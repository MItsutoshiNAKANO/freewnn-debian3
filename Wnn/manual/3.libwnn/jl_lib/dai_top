


JL_DAI_TOP( )       UNIX Programmer's Manual        JL_DAI_TOP( )



名称

     jl_dai_top()        大文節の先頭であるかどうかを判定する。

形式

     #include "jllib.h"

     int
     jl_dai_top(buf, k)
     struct wnn_buf *buf;          /* バッファ */
     int k;                        /* 文節番号 */

機能

     文節番号目の文節が大文節の先頭であるかどうかを判定する。

リターンバリュー

     文節番号目の文節が大文節の先頭であれば、1 を返す。
     文節番号目の文節が大文節の先頭でなければ、0 を返す。




Printed 1/22/92         19 September 1991                       1



































































































