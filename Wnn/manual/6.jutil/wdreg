


WDREG( )            UNIX Programmer's Manual             WDREG( )



名称

     wdreg               UJISテキストを元に、単語登録を行なう。

形式

     /usr/local/bin/Wnn4/wdreg [-D ホスト名] [-n 環境名] [-d 辞書番号]
             [-L] [ファイル名] < テキスト

機能

     指定された jserver の環境の辞書に、 UJIS 形式の辞書 text の
     中の単語をバッチ的に登録する。


     以下のオプションによって、動作を指定する。

     -D ホスト名
             サーバのホスト名を指定する。指定されない場合、環境変
             数 JSERVER が参照される。これもない場合は、
             UNIX_domain が使われる。

     -n 環境名
             環境名を指定する。デフォルトは "wdreg" である。

     -d 辞書番号
             辞書のシリアル番号を指定する。ファイル名と、このオプ
             ションは同時には指定できない。ファイル名かこのオプシ
             ョン、どちらかを指定する。

     -L      クライアント側の辞書が参照される。



Printed 1/28/92          28 January 1992                        1


























































































