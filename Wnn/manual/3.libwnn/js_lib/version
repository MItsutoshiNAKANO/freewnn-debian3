


JS_VERSION( )       UNIX Programmer's Manual        JS_VERSION( )



名称

     js_version()        サーバ、及び、ライブラリのバージョンを調べる。

形式

     #include "jslib.h"

     int
     js_version(server,serv,libv)
     int *serv;                    /* サーバのバージョン */
     int *libv;                    /* ライブラリのバージョン */
     WNN_JSERVER_ID *server;       /* サーバのID */

機能

     サーバ ID server のサーバ、及び、ライブラリのバージョンを調
     べ、それぞれ serv 、 libv に返す。

     91 年 10 月 1 日現在、バージョンは、サーバのバージョンが
     Ox4100, ライブラリのバージョンが Ox4003 である。

リターンバリュー

     正常終了時には、0 を返す。
     異常終了時には、-1 を返す。




Printed 1/22/92          1 October 1991                         1






























































































