


WNNKILL( )          UNIX Programmer's Manual           WNNKILL( )



名称

     wnnkill             サーバを終了させる。

形式

     /usr/local/bin/Wnn4/wnnkill [-J|U|S] [-L] [hostname]

機能

     hostname で示されるマシンのサーバを使用しているクライアント
     がいる場合、サーバの使用者数を返す。 hostname で示されるマシ
     ンのサーバを使用しているクライアントがない場合、サーバを終了
     させる。 hostname が省略された場合は、 UNIX-Domain のサーバ
     を調べる。

     以下のオプションにより動作を指定する。

     -J      JIS コードで出力する。

     -U      UJIS コードで出力する。 ( デフォルト )

     -S      SJIS コードで出力する。

     -L      LANG 名を指定する。 zh_CN を指定した場合、 cserver
             が対象となる。デフォルトは、 ja_JP つまり jserver で
             ある。

Printed 1/28/92          28 January 1992                        1
































































































